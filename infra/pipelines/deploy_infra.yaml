trigger: none

parameters:
  - name: personName
    displayName: "Person/Owner Name"
    type: string
    [cite_start]default: 'Arthur' # [cite: 2]
  - name: location
    displayName: "Azure Region"
    type: string
    [cite_start]default: 'brazilsouth' # [cite: 2]
  - name: templateML
    type: string
    [cite_start]default: 'bicep_files/azure_ml_deploy.bicep' # [cite: 2]
  # Selection parameters for stages
  - name: runDev
    displayName: "Deploy to Dev?"
    type: boolean
    default: true
  - name: runTest
    displayName: "Deploy to Test?"
    type: boolean
    default: false
  - name: runProd
    displayName: "Deploy to Prod?"
    type: boolean
    default: false

variables:
  - [cite_start]group: Arthur_VG # Contains service_conn and subscription_id [cite: 2]
  - name: rgDev
    value: 'rg-dev-${{ parameters.personName }}'
  - name: rgTest
    value: 'rg-test-${{ parameters.personName }}'
  - name: rgProd
    value: 'rg-prod-${{ parameters.personName }}'

stages:
  - stage: Lint
    displayName: 'Lint Bicep'
    jobs:
      - job: LintBicep
        steps:
          - script: az bicep build --file ${{ parameters.templateML }}
            displayName: 'Bicep Lint'

  # --- DEV STAGE ---
  - stage: Deploy_Dev
    displayName: 'Deploy to Development'
    dependsOn: Lint
    # Only run if runDev is true AND Lint succeeded
    condition: and(succeeded(), eq(${{ parameters.runDev }}, true))
    variables:
      envName: 'dev'
      rgName: $(rgDev)
    jobs:
      - job: DeployJob
        steps:
          - task: AzureCLI@2
            displayName: 'Ensure Resource Group Exists'
            inputs:
              azureSubscription: '$(service_conn)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: az group create --name $(rgName) --location ${{ parameters.location }}

          - task: AzureCLI@2
            displayName: 'Deploy Resources'
            inputs:
              azureSubscription: '$(service_conn)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: |
                WORKSPACE_NAME=$(az deployment group create \
                  --resource-group $(rgName) \
                  --template-file ${{ parameters.templateML }} \
                  --parameters personName='${{ parameters.personName }}' \
                               environment='$(envName)' \
                               location='${{ parameters.location }}' \
                  --query properties.outputs.mlWorkspaceName.value -o tsv)
                echo "##vso[task.setvariable variable=workspaceName]$WORKSPACE_NAME"

          - task: AzureCLI@2
            displayName: 'Smoke Test'
            inputs:
              azureSubscription: '$(service_conn)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: az ml workspace show --name $(workspaceName) --resource-group $(rgName)

  # --- TEST STAGE ---
  - stage: Deploy_Test
    displayName: 'Deploy to Test'
    dependsOn: Deploy_Dev # Sequential dependency
    # Run if runTest is true AND previous stages didn't fail (even if they were skipped)
    condition: and(not(failed()), eq(${{ parameters.runTest }}, true))
    variables:
      envName: 'test'
      rgName: $(rgTest)
    jobs:
      - job: DeployJob
        steps:
          - task: AzureCLI@2
            displayName: 'Ensure Resource Group Exists'
            inputs:
              azureSubscription: '$(service_conn)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: az group create --name $(rgName) --location ${{ parameters.location }}

          - task: AzureCLI@2
            displayName: 'Deploy Resources'
            inputs:
              azureSubscription: '$(service_conn)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: |
                WORKSPACE_NAME=$(az deployment group create \
                  --resource-group $(rgName) \
                  --template-file ${{ parameters.templateML }} \
                  --parameters personName='${{ parameters.personName }}' \
                               environment='$(envName)' \
                               location='${{ parameters.location }}' \
                  --query properties.outputs.mlWorkspaceName.value -o tsv)
                echo "##vso[task.setvariable variable=workspaceName]$WORKSPACE_NAME"

          - task: AzureCLI@2
            displayName: 'Smoke Test'
            inputs:
              azureSubscription: '$(service_conn)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: az ml workspace show --name $(workspaceName) --resource-group $(rgName)

  # --- PROD STAGE ---
  - stage: Deploy_Prod
    displayName: 'Deploy to Production'
    dependsOn: Deploy_Test # Sequential dependency
    condition: and(not(failed()), eq(${{ parameters.runProd }}, true))
    variables:
      envName: 'prod'
      rgName: $(rgProd)
    jobs:
      - job: DeployJob
        steps:
          - task: AzureCLI@2
            displayName: 'Ensure Resource Group Exists'
            inputs:
              azureSubscription: '$(service_conn)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: az group create --name $(rgName) --location ${{ parameters.location }}

          - task: AzureCLI@2
            displayName: 'Deploy Resources'
            inputs:
              azureSubscription: '$(service_conn)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: |
                WORKSPACE_NAME=$(az deployment group create \
                  --resource-group $(rgName) \
                  --template-file ${{ parameters.templateML }} \
                  --parameters personName='${{ parameters.personName }}' \
                               environment='$(envName)' \
                               location='${{ parameters.location }}' \
                  --query properties.outputs.mlWorkspaceName.value -o tsv)
                echo "##vso[task.setvariable variable=workspaceName]$WORKSPACE_NAME"

          - task: AzureCLI@2
            displayName: 'Smoke Test'
            inputs:
              azureSubscription: '$(service_conn)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: az ml workspace show --name $(workspaceName) --resource-group $(rgName)